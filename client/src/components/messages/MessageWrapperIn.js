import styled from 'styled-components';

const MessageWrapperIn = styled.div`
  width: 100vw;
  height: auto;
  display: flex;
  flex-flow: row nowrap;
  padding-left: 1.25rem;
`;

export default MessageWrapperIn;
